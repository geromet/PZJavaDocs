[pytest]
markers =
    restore: URL serialization and restoration checks
    recent: recently viewed classes persistence and navigation checks
    prefetch: hover prefetch behavior checks
    loading_state: inspectable loading state checks
    failure: failure-surface checks
    diagnostics: DOM diagnostics and error-state checks
    header: compact header and filter control checks
    breadcrumbs: breadcrumb rendering and navigation checks
    layout: stable detail and source panel layout checks
